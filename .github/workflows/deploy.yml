# ─────────────────────────────────────────────────────────────────
# DEAKTIVIERT — 2026-03-17
# Dieser Workflow war für Docker/SSH-Deployment.
# Das Frontend (apps/web) wird jetzt über Vercel deployed.
# Das Backend (apps/api) wird separat über Railway/Render deployed.
#
# Dieser Workflow läuft NICHT mehr automatisch (kein on:-Trigger).
# Er ist archiviert und kann bei Bedarf reaktiviert werden.
# ─────────────────────────────────────────────────────────────────
name: "[ARCHIVIERT] Deploy — Docker/SSH"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Nur manuell ausführen wenn Docker-Deploy gewünscht"
        required: true
        default: "nein"

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Hinweis
        run: |
          echo "Dieser Workflow ist archiviert."
          echo "Frontend: Vercel (automatisch via GitHub Push)"
          echo "Backend:  Railway/Render (separates Repository oder Service)"
